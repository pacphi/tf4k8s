locals {
  kubeconfig_path = "</path/to/kubeconfig>"
  ytt_lib_dir = "${path.module}/../../../ytt-libs"
}

module "nginx_ingress" {
  source = "../../nginx-ingress"
  ytt_lib_dir      = var.local.ytt_lib_dir

}

provider "k14s" {
  kapp {
    kubeconfig_yaml = file(local.kubeconfig_path)
  }
}

